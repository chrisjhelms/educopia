#!/usr/bin/python
import sys
import time 
try: 
	tm = int(sys.argv[1]) / 1000 
        print tm
	print time.strftime("%a, %d %b %Y %H:%M:%S +0000", time.localtime(tm)) 
except  (ValueError, IndexError): 
	print "must give epoch time value"
